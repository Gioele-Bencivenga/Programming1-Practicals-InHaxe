class Bob{
    var currentAge:Int;
    var retirementAge:Int;
    var startingSalary:Int;
    var yearlySalaryIncreaseRate:Int; //salary increases at the end of every year
    var monthlyMortgageAmount:Int;
    var paidMortgage:Int;
    var totalDueMortgage:Int;
    var yearlyMortgageAmount:Int;
    var monthlyUtilitiesAmount:Int;
    var yearlyUtilitiesAmount:Int;
    var savings:Int;
    var taxedSalary:Int;
}